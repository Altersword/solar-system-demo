# 开发日记

## 2026-05-16 · Bloom 后处理

### 新增
- `index.html` 引入 6 个 Three.js 后处理 CDN 脚本
- `solarSystem.js` 新增 EffectComposer + UnrealBloomPass 管线
  - `setupBloom()` — 创建 RenderPass + UnrealBloomPass
  - `animate()` — 近景模式走 `composer.render()`，普通星图走 `renderer.render()`
  - `showFocusView()` — 启 bloom，强度来自 `blackHoleConfig.bloomStrength`
  - `clearFocusView()` — 关 bloom
  - `onWindowResize()` — 同步 composer 尺寸

### Bloom 参数
- strength: 1.3, radius: 0.5, threshold: 0.2
- 只作用于亮度 > 0.2 的像素（内沿白黄、中盘橙黄），暗区不 bloom

---

## 2026-05-16 · 黑洞 GPU shader 坐标系统一 + 去环攻关（全天记录）

### 起点
- GPU shader 与 CPU 渲染器坐标系不一致
- GPU 用 rossning92（y=spin, xz=disk, EH=1, M隐式=1）
- CPU 用 USTC（z=spin, xy=disk, EH=0.4, M=0.2）
- 两者参数无法互通，混合渲染做不了

---

### 第一轮：坐标系统一

**blackHoleRenderer.js 完全重写**：
- z=spin, xy=disk, M=0.2, EH=0.4
- Three.js→Schw 映射：x→x, z→y, y→z（与 CPU `computeCameraParams` 完全一致）
- 测地线方程加入 M：`vel += -1.5 * M * h² * pos / r⁵ * dt`
- 相机基向量：spin 轴 `(0,0,1)`，right = forward × z_axis
- 新增 USTC 多普勒：g=√((r-3M)/(r-2M)), β=√(M/(r-2M))

**solarSystem.js**：黑洞近景相机 `(40,3,40)` → `(30,25,30)`

---

### 第二轮：圆环攻坚战（5 次迭代）

**问题诊断**：vision.js 分析截图，发现 20-30 层规则同心圆环（"唱片纹路"）

**根因 1**：fbm 噪声的 hash 函数在 `diskR*5` 坐标上每 0.35 半径单位重复 → 16-17 道环 → **彻底移除 fbm**

**根因 2**：Doppler 在内沿近 ISCO 区域产生 1178:1 亮度比 → 右侧纯白过曝 → **DISK_IN 推到 1.2（ISCO），Doppler 从 USTC 四速投影改为线性** `1+2.5βcosθ`，钳制 [0.2, 4.5]

**根因 3**（最深）：「所有像素用相同步长采样 → 规律相干叠加 → Moiré 纹」
- **修复**：双随机抖动（起点 jitter + 步长 jitter）打破跨像素相干
- 加密采样：STEP 0.05, 420 步
- vision.js 确认圆环从 20-30 层→15-20 层→彻底消除

**体积渲染尝试**：opacity 累积替代加权平均，但盘太薄（DISK_HALF=0.25）导致光线错过盘面→全黑。回退到加权平均。

**最终去环方案**：
- 双随机抖动（每像素独立起点 + 步长 ±15%）
- 细步长 0.05 / 420 步
- 加权平均 + 径向剖面（陡内沿 + 1/r + 软外沿）
- 纯黑 EH（fellIn 像素不加抖色）
- 深空底色替代纯黑背景

---

### 当前 shader 状态

```
坐标系：z=spin, xy=disk, M=0.2, EH=0.4
盘面：DISK_IN=1.2(ISCO), DISK_OUT=7.0, DISK_HALF=0.38
步进：STEP=0.05, MAX_STEPS=420, 双随机抖动
Doppler：线性 1+2.5βcosθ, 钳制[0.2, 4.5]
色温：heat() 5段暖金→白→蓝白
亮度：加权平均 ×1.6
背景：深空暗蓝 (0.004, 0.003, 0.008)
EH：纯黑，柔边 smoothstep(0.34, 0.54)
```

### 参考试金石
- 中科大 BlackHole_STD：1800步 compute shader，极薄盘 `exp(-z²*655)`
- rossning92：300步 fragment shader，厚盘 + 高密度乘数 16000

### 后续方向
- CPU 高画质渲染校准 GPU 参数
- 引力透镜弧（光线绕黑洞背后的二次盘面交叠）
- 清理 solarSystem.js 旧 Three.js 几何代码
- 近景参数 UI 面板

---

## 2026-05-16 · 黑洞 GPU 渲染器重写 — USTC 物理移植

### 背景

之前的 `blackHoleRenderer.js` 和 `blackHoleCPU.js` 已被删除（"改成了一坨"）。需要从零重建 GPU 渲染器。

### 决策：Web 技术栈，不复刻 OpenGL

- 项目主体是 Three.js 太阳系星图，切到 C++/OpenGL 等于推翻重建
- USTC 效果好不是因为 OpenGL，而是 Compute Shader + 1800步 + 多pass Bloom
- WebGL 1.0 fragment shader 做单 pass 体渲染，用已有的 UnrealBloomPass 后处理
- 对标 USTC 图14 暖色版（Bloom=0.819, Temp=8751K, Doppler 全关）

### 第1轮：创建 blackHoleRenderer.js（353行）

**移植 USTC `volume_compute.comp` 的核心物理**：
- `radiation_color()` — Planckian 色温映射（1000K–40000K），逐行复刻
- `dopplerI()` / `dopplerT_factor()` — GR 多普勒公式，逐行复刻
- 测地线积分 `vel += -1.5*M*h²*pos/r⁵*dt`
- 极薄盘 `exp(-z²*655)` + 3D 值噪声

**坐标系**：z=spin, xy=disk（同 USTC）
**Three.js→shader 映射**：`_toShaderSpace(v) = (v.x, v.z, v.y)` — Three.js y→shader z (spin)
**参数**：M=1 (无量纲), EH=2, 初始 MAX_STEPS=400

**集成 solarSystem.js**：
- `blackHoleConfig` 新增 `diskTemperature`、`logLuminance`、`dopplerTemperature`
- CPU 渲染器路径 try-catch 包裹（`BlackHoleCPURenderer` 未重建）

### 第1轮结果：黑屏，只看到背景星空

**根因1**：`depthTest: false` 导致 quad 不写深度缓冲，星空粒子（`depthWrite: false`）渲染在后 → 盖住了 quad
**根因2**：`renderOrder: -999` 比星空提前渲染

**修复**：`depthTest: true, depthWrite: true, renderOrder: 998`

### 第2轮：仍然黑屏 + shader 编译失败

控制台报 `[BH] Shader compilation failed!`。Intel Arc + ANGLE (GLSL→HLSL) 对 400 步循环容忍度低。

**修复**：
- `MAX_STEPS 400→200`，改为 `const int` 不用 `#define`
- `bool fellIn→int fellIn`（ANGLE 部分版本对循环内 bool 有 bug）
- 移除 `vec3` 批量 `pow`，改用逐分量
- 移除 NaN 检测 `color.r != color.r`

### 第3轮：shader 编译通过 + 彩色诊断测试

临时用 `gl_FragColor = vec4(vUv, 0.5+0.5*sin(uTime), 1.0)` 验证，用户确认看到满屏彩色渐变 → **管线正常**

### 第4轮：恢复物理渲染 → 黑屏

**诊断**：相机距离盘面太远，200 步路径不够到达 z=0
- 相机在 shader 空间 z=12.5，dz≈0.51，需要 24.5 单位才到盘面
- 200*0.08=16 单位路径，不够

**修复**：
- 相机缩放 0.5→0.25，shader 空间 z=6.25
- MAX_STEPS 200→300, STEP 0.08→0.09，路径 27 单位

### 第5轮：300 步编译失败 → 回退 200 步 + 伪彩色调试

用 `minR` 输出伪彩色（红=远离BH，蓝=靠近，黑=落入EH），结果黑屏 → 证实 300 步编译失败

### 第6轮：二分法砍 shader — 定位 ANGLE 死代码优化

**关键发现**：ANGLE (GLSL→HLSL) 不会优化掉死代码！

- UV 渐变测试（`return;` 在第一行）能编译，是因为复杂函数 `diskDensity`/`blackbody`/`doppler` 在 `return` 之后，ANGLE 把它们优化掉了
- 去掉 `return` 后，函数变成可达代码 → ANGLE 编译超时/失败 → 黑屏
- **证实**：不是 200 步循环的问题，是函数定义太多（即使不调用也不安全！）

**第6.1轮**：砍到 30 行 — 只有 200 步循环 + minR 伪彩色，无任何函数
→ **成功！**用户确认看到 "由外向内由蓝到紫再到白" 的黑洞影子
→ 证实 200 步测地线积分 + 引力透镜在 Intel Arc 上正常工作

### 第7轮：逐步加物理 — 吸积盘检测

**第7.1轮**：加盘检测 `abs(z) < 0.08 && r in [6, 28]`，三色输出（黑=EH，橙=盘，蓝=背景）
→ 结果：只有蓝色球 + 黑色背景，**无橙色**（盘完全没检测到）

**第7.2轮**：放宽盘厚到 1.0，去掉径向限制，加诊断色（青=r<3, 绿=3<r<6, 黄=6<r<12, 橙=r>12）
→ 用户上下移动相机依次看到所有颜色 → **盘检测确认有效！光线确实穿越 z=0 平面**

**第7.3轮**：收薄盘厚到 0.08 + 纯橙色 → 看到橙色盘！首次看见吸积盘
**第7.4轮**：加垂直密度 `exp(-z²*655)` → 橙色宽线（编译成功）
**第7.5轮**：加径向高斯 + 内沿截断 → 橙色变窄（编译成功）
**第7.6轮**：加体积累积（alpha accumulation）→ 编译失败
**第7.7轮**：回退到 maxDens + 尝试加暖色渐变 → 编译失败
**第7.8轮**：回退 + 尝试加 smoothstep EH 边缘 → 编译失败
**第7.9轮**：回退 + 尝试 clamp 手算 EH 边缘 → 编译失败

### 第7轮根因发现：`uLuminance` uniform 未在 GLSL 中声明！

在第7.6-7.9轮的失败版本中，shader 输出段使用了 `uLuminance` 变量：
```glsl
float bright = clamp(maxDens * uLuminance, 0.0, 1.0);
```
但 GLSL `uniform` 声明行只有 `uAspect, uFovScale, uM, uTime`，**缺少 `uLuminance`**！

GLSL ES 1.00 要求所有使用的 uniform 必须显式声明。`uLuminance` 未声明 → 编译静默失败 → Three.js 回退到透明/黑色材质。

**这不是 ANGLE 指令预算问题！** 之前归因于 ANGLE 限制的多次失败，实际上可能是同一个 uniform 声明遗漏导致的。

### 第8轮：根因确认 + 逐功能验证

**根因**：`uLuminance` 在 JS `uniforms` 对象中定义了，但 GLSL 里没声明 `uniform float uLuminance;`。GLSL ES 1.00 要求所有使用的 uniform 必须显式声明，否则编译静默失败 → Three.js 回退黑色材质。

**之前 4-5 轮失败都是这个 bug**，不是 ANGLE 指令预算问题！

### 逐步骤验证（每步均刷新确认）

| 步骤 | 改动 | GLSL uniform 声明 | 结果 |
|------|------|-------------------|------|
| 8.1 | 基线：200步 + 盘密度(maxDens) + 纯橙色 | 只声明了 uAspect,uFovScale,uM | ✓ 橙色盘 |
| 8.2 | +peakR 追踪 | 同上 | ✓ 橙色盘 |
| 8.3 | +r-dependent 暖色（纯算术，无分支） | 同上 | ✓ 黄→红渐变 |
| 8.4 | +EH 边缘软化（clamp 手算） | 同上 | ✓ 软边缘 |
| 8.5 | +uLuminance 亮度控制 | **加 `uniform float uLuminance;`** | ✓ 盘变亮（过曝） |
| 8.6 | 调低 uLuminance 6.8e6→2.5 | 同上 | ✓ 有层次感的暖色盘 |

### 第9轮：并行加功能 → 黑屏 → 逐个验证

**一次加 smoothstep + 噪声 + 体积累积 + Planckian → 黑屏**

开始逐个验证，步骤：
- **9.1**：回退基线 + smoothstep → 等待明天验证

### 当前状态（2026-05-17 凌晨）

| 组件 | 状态 |
|------|------|
| 200步测地线积分 | ✓ 通过 |
| 管线 (quad + 坐标映射) | ✓ 通过 |
| 极薄盘 `exp(-z²*655)` | ✓ 通过 |
| 径向高斯 + ISCO 截断 | ✓ 通过 |
| 暖色渐变（纯算术） | ✓ 通过 |
| `uLuminance` 亮度 | ✓ 通过（注意：必须 GLSL 声明） |
| `smoothstep` EH 边缘 | **等待验证** |
| 噪声纹理 | 待验证 |
| 体积累积 `accum += ...` | 待验证 |
| Planckian 色温 | 待验证 |
| 多普勒效应 | 待添加 |
| Bloom 后处理 | 已有 UnrealBloomPass |

### 明天继续

从 smoothstep 验证开始 → 逐个加噪声/累积/Planckian → 全部通过后做对比截图 → 更新提示词工程

---

## 2026-05-17 · 黑洞渲染推倒重来 — 独立测试项目

### 背景

Claude Opus (effort=max) 接过项目，完整浏览了所有 markdown 和源码后，在 `blackHoleRenderer.js` 中做了增强：
- 加入 hash 噪声湍流
- 加入多普勒增亮（开普勒切向速度投影）
- 加入 Planckian 色温渐变（5 段 smoothstep 混合）
- 从 `maxDens` 改为 `totalDens` 累积

### Intel Arc 上首次编译成功

增强版 shader 在 Intel Arc + ANGLE 上一次编译通过（`[BH] GPU shader created` + `[BH] GPU shader ready`），证明之前的编译失败根因确实是 `uLuminance` uniform 未声明，而非指令预算不足。

### 致命 bug：盘面只显示一条线

增强后盘面从细线变粗线，但仍不是椭圆盘结构。**根因**在 `update()` 的坐标变换：

```
quad.lookAt(camera)            → quad 永远正对相机
invWorld = quad.matrixWorld.invert()
camLocal = camera.position × invWorld → 永远是 (0,0,d)
camSw = swap(camLocal)         → (0,d,0)
```

相机被锁定在 shader 空间的盘面内 (z=0)，视角永远侧视边缘 → 只能看到一条线。

修复方案：不用 quad 局部坐标，直接传世界空间相机位置。但用户决定推倒重来。

### 推倒重来

1. **删除** `js/blackHoleRenderer.js`
2. **还原** `index.html`（去掉 blackHoleRenderer.js 引用）
3. **还原** `solarSystem.js`：
   - 恢复 `dopplerIntensity: 0.0`
   - 删除 `createFocusedBlackHoleModel()` 和 `triggerBlackHoleRender()`
   - 黑洞近景回到 `createFocusedSpecialModel` 的 Three.js 几何开关分支
   - 删除 `showFocusView` 中黑洞专属 bloom/camera 逻辑
   - 简化 `clearFocusView`

### 新建独立项目 `测试黑洞/`

`测试黑洞/index.html` — 单文件、Three.js ES module、零依赖：

**架构**：
- Three.js `PlaneGeometry(2,2)` full-screen quad + `OrthographicCamera`
- 手动球坐标轨道控制（鼠标拖拽旋转、滚轮缩放）
- 相机世界坐标直接传 shader（swap: Three.js Y→shader Z=自旋轴）
- 200 步 Schwarzschild 测地线积分
- `totalDens` 累积采样（非 maxDens）
- Planckian 5 段色温渐变
- 多普勒增亮（可调 0–1）
- 多尺度 hash 湍流
- 底部实时 UI 滑块（多普勒、亮度、色温）

**坐标系**：Three.js Y-up = 自旋轴，shader 内 Z=自旋、XY=盘面，经 `swap(x,y,z)→(x,z,y)` 映射。

**未验证**：独立项目刚写完，未在浏览器中实际测试。需要确认盘面是否显示为倾斜椭圆而非线条。

### 给下一轮的建议

- 在 `测试黑洞/` 目录 `python -m http.server 4173` 打开
- 先确认盘面能正确显示（倾斜视角下是椭圆，不是线）
- 如果仍是线：检查 swap 映射和相机基向量计算
- 效果对了之后：加引力透镜（上下弧）、光子环、bloom 后处理

---

## 2026-07-12 · 架构拆分（方案 A）

### 完成

- 删除 `solarSystem.js` 旧黑洞死代码（约 900 行未调用 shader 路径）
- 纳入并迁入 `js/specials/blackHoleRenderer.js`（唯一黑洞近景管线）
- specials: `PulsarRenderer` / `SimpleSpecialsRenderer` / `SpecialBodyFactory`
- focus: `FocusController`（近景 show/clear、场景显隐、接管渲染）
- core: `AppContext` / `BloomComposer`
- solar: `SolarBodies`
- atlas: `StarAtlas`
- `SolarSystem` 变为薄门面（约 1288 行，含共享 helpers）

### 管线

- 黑洞近景：`SpecialBodyFactory` → `BlackHoleRenderer`（ownsScreen）
- 脉冲星近景：`PulsarRenderer` + host 特效更新
- 其他特殊天体：`SimpleSpecialsRenderer`
- 近景生命周期：`FocusController` + `SpecialBodyFactory`

### 未做

- ES Module / Vite
- 黑洞物理公式重写 / 视觉继续打磨
- 共享 helpers（材质、标签、dispose）仍暂留在 `solarSystem.js`

---

## 2026-07-13 · 星图扩充

### 完成
- 提交 atlas 基础数据与 UI（`planetData` / `main` / CSS）
- `CELESTIAL_CATALOG` 从 35 → **60**
  - neighborhood 16：新增拉兰德 21185、沃尔夫 359、罗斯 154、格利泽 581、天鹅座 61、牛郎星等
  - milky-way 24：新增心宿二、参宿七、天津四、北极星、半人马座 ω、杜鹃座 47、毕星团、仙后座 A、船底座 η 等
  - local-group 10：新增 M32、M110、IC 10、天炉座矮星系、狮子座 I
  - cosmic-neighborhood 10：新增天炉座/后发座/英仙座星系团、长蛇-半人马与巨引源方向
- 四层尺度距离壳（`starAtlas.createDistanceShells`）
- 图层感知 `scaleCatalogDistance`
- 浏览器验证：catalog=60，shells=14，四层切换与中文信息面板正常

### 提交
- `47d6ab7` atlas foundation
- `6daffcf` expand catalog + scale-aware shells

### 阶段 A · 近邻恒星与系外行星
- `CELESTIAL_CATALOG` 从 60 → **70**，其中 neighborhood 从 16 → **26**。
- 新增半人马座 α B、鲁坦 726-8、罗斯 128、蒂加登星、格利泽 667、开普勒-186、开普勒-452、LHS 1140、格利泽 1132、HD 219134。
- 系外行星系统复用现有 `systemLayout`，没有新增渲染器或交互分支。
- 目录校验通过：`Catalog valid: 70 entries`。
- 阶段 B 银河系地标深化尚未开始，下一步先做浏览器交互验收。

### 阶段 B · 银河系地标深化
- `CELESTIAL_CATALOG` 从 70 → **80**，其中 milky-way 从 24 → **34**。
- 新增猎户臂方向、盾牌-半人马臂方向、银河系核球方向标，以及 M13、M3、NGC 604、NGC 3603、WR 104、手枪星和船帆座超新星遗迹。
- 结构目标明确标记为方向/区域参照，不伪造银河旋臂实体几何；所有目标复用现有目录分类和渲染路径。
- 目录校验通过：`Catalog valid: 80 entries`。
- 阶段 C 本星系群与宇宙近邻扩展尚未开始。

### 阶段 C · 本星系群与宇宙近邻
- `CELESTIAL_CATALOG` 从 80 → **90**。
- local-group 从 10 → **15**：新增 IC 1613、巴纳德星系、WLM、仙女座 II、仙女座 III。
- cosmic-neighborhood 从 10 → **15**：新增 NGC 253、M82、半人马座 A、M87、本超星系团方向。
- 单星系目标复用现有 galaxy 渲染路径；本超星系团继续作为方向和尺度参照，不绘制虚假实体边界。
- 目录校验通过：`Catalog valid: 90 entries`。
- 下一独立任务：优化船帆座脉冲星与船帆座超新星遗迹的视觉模型。
